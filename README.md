# rag-based-chatbot-
A Streamlit-based Retrieval-Augmented Generation (RAG) app that lets users chat with their PDFs, DOCX, and TXT files using FAISS vector search + HuggingFace embeddings + Groq LLM (LLaMA 3). It retrieves relevant document chunks and generates grounded, hallucination-free answers.
